// ═══════════════════════════════════════════════════════════
// ملف بيانات إعدادات قاعدة البيانات — يُحدَّث تلقائياً من لوحة التحكم
// عند ربط قاعدة بيانات جديدة من صفحة الإدارة يتم إعادة كتابة هذا الملف
// من الخادم لكي يعمل الموقع لجميع الزوار.
// ملاحظة: لا توجد قاعدة بيانات افتراضية مضمّنة — القيمة null تعني عدم وجودها.
// لا تعدّل هذا الملف يدوياً.
// ═══════════════════════════════════════════════════════════
window.__ELMISTAR_EMBEDDED_CONFIG = null;
