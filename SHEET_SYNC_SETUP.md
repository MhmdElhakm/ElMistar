# دليل ربط ElMistar بجوجل شيت (مزامنة ثنائية الاتجاه)

هذا الدليل يربط قاعدة بيانات فايرستور (Firestore) الخاصة بموقع ElMistar بجوجل شيت واحد، بحيث يمكنك عرض وتعديل الطلاب وطلبات الالتحاق مباشرة من الشيت.

> ⚠️ **قبل البدء تأكد أن الموقع مربوط بقاعدة بيانات صحيحة**: افتح لوحة التحكم (`admin.html`) وتأكد من ربط مشروع Firebase الصحيح. رقم `Project ID` الذي يظهر في شاشة الربط هو نفس الرقم الذي ستدخله في الخطوة 4 أدناه.

---

## 1) إنشاء مفتاح حساب خدمة (Service Account) من Firebase

1. افتح [Firebase Console](https://console.firebase.google.com) وادخل على **مشروعك الصحيح** (نفس المشروع المربوط بالموقع).
2. من القائمة الجانبية: **⚙️ إعدادات المشروع ← علامة التبويب «حسابات الخدمة» (Service accounts)**.
3. اضغط **«إنشاء مفتاح خاص جديد» (Generate new private key)** ثم **«توليد المفتاح» (Generate key)**.
4. سينزّل ملف JSON باسم مشابه لـ `اسم-المشروع-firebase-adminsdk-xxxx.json`. **احتفظ به في مكان آمن ولا تشاركه مع أي شخص** — ستحتاج فقط إلى محتواه (نص JSON) في الخطوة 4.

> 🔑 هذا المفتاح يعطي صلاحية الوصول إلى قاعدة البيانات بالكامل، لذلك لا ترفعه لأي مكان ولا تضعه في الكود. هنا سيُحفظ داخل إعدادات السكريبت نفسه (مشفّرًا Base64) ولن يظهر في أي ملف.

### منح صلاحية الكتابة (خطوة مهمة)

حساب الخدمة الجديد يحتاج صلاحية على Firestore. أسهل طريقة (من إعدادات المشروع ← «أذونات IAM»):

- ابحث عن البريد `xxx@xxx.iam.gserviceaccount.com` الخاص بالحساب، وامنحه دور **Cloud Datastore User** (أو دور **Owner/Editor** إذا أردت الأبسط).

إذا لم تُمنح الصلاحية ستظهر رسالة `Permission denied` عند الربط أو السحب.

---

## 2) إنشاء جدول Google Sheets فارغ

1. افتح [Google Sheets](https://sheets.google.com) وأنشئ جدولًا جديدًا فارغًا (احذف الورقة الأولى `Sheet1` أو اتركها — ستنشئ السكريبت الأوراق الصحيحة).
2. من القائمة: **«الإضافات» ← «Apps Script»** (أو من القائمة **Extensions → Apps Script**).

---

## 3) لصق كود المزامنة في Apps Script

1. في محرر Apps Script امسح أي محتوى موجود، ثم انسخ **محتوى الملف `google-sheets-sync.gs`** الموجود في مجلد المشروع كاملًا والصقه.
2. اضغط 💾 **حفظ (Save)** ثم سجّل دخولك إذا طُلب.
3. **أعد فتح الجدول** (نافذة الشيت) لتحديث القائمة. ستظهر قائمة جديدة في شريط الأدوات باسم:

   > **🔥 مزامنة فايرستور**

---

## 4) ربط الإعدادات (Project ID + مفتاح حساب الخدمة)

من قائمة **🔥 مزامنة فايرستور** اضغط **«1️⃣ ربط الإعدادات (Project + Service Account)»**:

1. أدخل **Project ID** — من شاشة ربط قاعدة البيانات في لوحة تحكم الموقع، أو من Firebase Console (يظهر أعلى الإعدادات، وليس الاسم الظاهر بل الرقم المميّز مثل `mystar-site`).
2. الصق **محتوى ملف JSON بالكامل** (نص الملف كما هو، من أول `{` إلى آخر `}`).
3. سيتحقق السكريبت من الاتصال، وإذا نجح سيظهر: **«✅ تم حفظ الإعدادات والتحقق من الاتصال»**.

> إذا ظهرت رسالة فشل اتصال، تحقق من الخطوة 1/منح الصلاحية (Cloud Datastore User).

---

## 5) إنشاء صفحات الشيت

من القائمة اضغط **«2️⃣ إنشاء صفحات الشيت»**.

سيُنشئ 7 أوراق:
- أوراق الصفوف: **الصف الأول الابتدائي** … **الصف السادس الابتدائي** (بعلامات تبويب خضراء).
- ورقة: **طلبات الالتحاق** (برتقالية).

مع الترويسات والقوائم المنسدلة التلقائية (الصف، مستوى الطلب، السنة، الحالة).

---

## 6) السحب الأول من فايرستور

اضغط **«3️⃣ سحب البيانات من فايرستور»**.

- تُملأ أوراق الصفوف بطلاب الصفوف من 1 إلى 6 فقط (الطلاب في مرحلة KG/التأسيس وغيرها لا يُصدَّرون).
- الطلاب بلا مجموعة تُترك خانات اليوم والساعة فارغة (يمكنك كتابتها لاحقًا).
- عمود **«المستحق»** محسوب تلقائيًا: `أكبر من (عدد الحصص المسجلة × سعر الحصة حسب السنة + مصاريف) − المدفوع` وأكبر من الصفر.
- عمود **«معرف»** (العمود A) هو معرّف المستند الداخلي — **لا تعدّله**. يمكنك إخفاؤه: اضغط يمينًا على العمود A ← «إخفاء العمود».

> 💡 إذا كانت بياناتك كثيرة قد تستغرق المرة الأولى دقيقة أو دقيقتين. لا تغلق الجدول أثناء السحب.

---

## 7) التعديل من الشيت

### التعديل الفوري (كل خلية)
- من القائمة اضغط **«تفعيل التعديل الفوري + سحب تلقائي كل 10 دقائق»**.
- الآن أي تعديل تكتبه في الخلايا المسموحة يُحفظ مباشرة في فايرستور (ثوانٍ).
- الأعمدة **«معرف»** و **«المستحق»** للقراءة فقط (المستحق يُحسب تلقائيًا — لا تكتب فيه يدويًا).

### الحالات الخاصة
| العمود | السلوك |
|---|---|
| اسم المجموعة | إذا كتبت اسم مجموعة غير موجودة، تُنشأ المجموعة تلقائيًا في فايرستور |
| اليوم / الساعة | إذا كان الطالب في مجموعة، يُعدَّل موعد **المجموعة نفسها** (ينطبق على كل طلابها) |
| مستوى الطلب | مفعّل / قيد الانتظار / مرفوض |
| السنة الدراسية | العام الحالي / العام الماضي / الكورس الصيفي |
| الحالة (طلبات الالتحاق) | قيد الانتظار / تم القبول / مرفوض / ملغي |

### الإرسال الكامل (أداة احتياطية)
إذا حصل أي خطأ فادح، اضغط **«4️⃣ إرسال كل التعديلات إلى فايرستور»** لدفع كل صفوف الشيت دفعة واحدة.

> ⚠️ **تحذير التعديل الفوري**: أي تعديل من الشيت يكتب فوق قيمة فايرستور مباشرة (آخر كاتب يفوز). إذا فتحت الشيت والمتصفح معًا وعدّلت نفس الخانة مرتين، تبقى القيمة الأخيرة. تجنّب التعديل المتزامن مع السحب التلقائي الذي يعمل كل 10 دقائق.

---

## 8) أعمدة الأوراق

### أوراق الصفوف (14 عمودًا)
| العمود | العنوان | قابل للتعديل؟ |
|---|---|---|
| A | المعرف (داخلي) | ❌ |
| B | رقم الهاتف | ✅ |
| C | الاسم | ✅ |
| D | الصف الدراسي | ✅ (قائمة منسدلة) |
| E | اسم المجموعة | ✅ (ينشئ مجموعة إن لم توجد) |
| F | اليوم | ✅ |
| G | الساعة | ✅ |
| H | عدد الحصص المسجلة | ✅ |
| I | إجمالي الحصص | ✅ |
| J | المدفوع | ✅ |
| K | المستحق (محسوب تلقائيًا) | ❌ |
| L | مستوى الطلب | ✅ (قائمة منسدلة) |
| M | ملاحظات المعلم | ✅ |
| N | السنة الدراسية | ✅ (قائمة منسدلة) |

### ورقة طلبات الالتحاق (8 أعمدة)
| العمود | العنوان | قابل للتعديل؟ |
|---|---|---|
| A | المعرف (داخلي) | ❌ |
| B | اسم الطالب | ✅ |
| C | رقم الهاتف | ✅ |
| D | الصف | ✅ |
| E | السنة الدراسية | ✅ |
| F | الحالة | ✅ (قائمة منسدلة) |
| G | سبب الرفض | ✅ |
| H | تاريخ الطلب | ❌ |

---

## 9) إيقاف المزامنة / حذف الإعدادات

- **إيقاف التعديل الفوري والسحب التلقائي**: من القائمة ← «إيقاف المؤقتات».
- **حذف الإعدادات المخزنة** (المشروع + المفتاح): من القائمة ← «حذف الإعدادات المخزنة». نفّذها قبل نقل الجدول لشخص آخر.

---

## استكشاف الأخطاء

| المشكلة | الحل |
|---|---|
| «فشل الاتصال: Firestore 403 Permission denied» | منح صلاحية Cloud Datastore User لحساب الخدمة (الخطوة 1) |
| «Project ID غير صحيح» | تأكد أن الرقم المدخل هو Project ID وليس اسم المشروع أو رقم Firebase |
| الصف لا يظهر في الشيت | تأكد أن قيمة الصف في فايرستور ضمن الصيغ المعروفة (مثل `3`, `g3`, `ثالث ابتدائي`) وأنه من الصف 1-6 |
| «هناك مزامنة جارية الآن» | انتظر دقيقة ثم أعد المحاولة |
| التاريخ/الحالة لا يتغيران من الشيت | هذه الأعمدة للقراءة فقط — عدّلها من لوحة التحكم |

---

## ملاحظات تقنية (للمطور)

- يستخدم السكريبت REST API الخاص بـ Firestore مباشرة (`projects/<projectId>/databases/(default)/documents`) مع JWT موقع يدويًا (RS256) بدون مكتبات خارجية.
- يُخزَّن مفتاح حساب الخدمة في Script Properties بصيغة Base64، وتوكن الوصول مؤقتًا في ScriptCache (50 دقيقة).
- السحب الكامل يعيد كتابة الأوراق من الصف 2 (تفراغ كامل ثم كتابة) مع إبقاء الترويسات.
- أسماء الصفوف تُوحَّد عبر `normalizeGrade` مثل لوحة التحكم تمامًا.
- سعر الحصة يُقرأ من `settings/sessionConfig` (افتراضي: حالي 15، ماضي 10، صيفي 20) ويُحتسب «المستحق» بنفس معادلة لوحة التحكم: `max(0, (attendanceCount × sessionPrice + otherExpenses) − paid)`.
